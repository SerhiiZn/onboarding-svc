apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onboarding-svc.fullname" . }}-test-connection"
  labels:
    {{- include "onboarding-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onboarding-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
