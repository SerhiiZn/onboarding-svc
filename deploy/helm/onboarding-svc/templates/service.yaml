apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-onboarding-svc
  namespace: serhiizn-dev
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-onboarding-svc
